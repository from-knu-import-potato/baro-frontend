import StoreSelectionCards from '@/features/store-registration/components/StoreSelectionCards';

const StoreSelectionPage = () => {
  return <StoreSelectionCards />;
};

export default StoreSelectionPage;
